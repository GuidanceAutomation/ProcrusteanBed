<UserControl x:Class="ProcrusteanBed.Core.Controls.AtomicMoveListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcrusteanBed.Core.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StatusBar>
            <TextBlock Text="{Binding Path=TaskType, FallbackValue='fb=TaskType'}"/>
            <StatusBar.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add Atomic Move" x:Name="addAtomicMoveMenuItem" Click="AddAtomicMoveMenuItem_Click"/>
                </ContextMenu>
            </StatusBar.ContextMenu>
        </StatusBar>
        <ItemsControl ItemsSource="{Binding Path=SubtasksOC}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:AtomicMoveControl/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
